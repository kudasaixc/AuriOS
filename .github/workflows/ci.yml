name: AuriOS CI

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    branches: ["main", "develop"]
    paths:
      - "src/**"
      - "Makefile"
      - "linker.ld"
      - ".github/workflows/ci.yml"
  pull_request:
    branches: ["main", "develop"]

  workflow_dispatch:

jobs:
  build-gcc:
    name: Build with GCC (i686-elf)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: add local bin to path
        run: echo "$PWD/.environment/bin" >> $GITHUB_PATH

      - name: restore cross compiler cache
        id: cache-toolchain
        uses: actions/cache@v4
        with:
          path: .environment
          key: ${{ runner.os }}-i686-elf-gcc-${{ hashFiles('Makefile', 'scripts/install.sh') }}

      - name: install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y nasm xorriso mtools grub-pc-bin grub-common

      - name: zig install
        uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0

      - name: build cross compiler
        if: steps.cache-toolchain.outputs.cache-hit != 'true'
        run: |
          sudo apt-get install -y gcc g++ binutils make wget tar texinfo libgmp-dev libmpfr-dev libmpc-dev
          chmod +x scripts/install.sh
          ./scripts/install.sh

      - name: create ISO
        run: |
          make clean
          make iso

      - name: upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: AuriOS-GCC-ISO
          path: output/AuriOS.iso
          retention-days: 5

  build-zig:
    name: Build with Zig Toolchain
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: add local bin to path
        run: echo "$PWD/.environment/bin" >> $GITHUB_PATH

      - name: install zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0

      - name: install ISO deps
        run: |
          sudo apt-get update
          sudo apt-get install -y nasm xorriso mtools grub-pc-bin grub-common

      - name: create ISO
        run: |
          make clean
          make USE_ZIG=1 iso

      - name: upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: AuriOS-Zig-ISO
          path: output/AuriOS.iso
          retention-days: 5

  integration-tests:
    name: Integration Tests (Walkman)
    runs-on: ubuntu-latest
    needs: build-zig

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: install system deps & QEMU
        run: |
          sudo apt-get update
          sudo apt-get install -y nasm xorriso mtools grub-pc-bin grub-common qemu-system-x86

      - name: install zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0

      - name: Build Test ISO (AURI_TEST_MODE)
        run: |
          make clean
          make USE_ZIG=1 iso-debug

      - name: Install Walkman
        run: |
          cargo install --git https://github.com/Auri-OS/walkman.git --tag v0.1.2

      - name: Run Walkman Test Suite
        run: |
          walkman tests/integrations --nui
